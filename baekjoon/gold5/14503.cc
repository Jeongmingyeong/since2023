#include <iostream>
#define MAX_SIZE 50

using namespace std;

int arr[MAX_SIZE][MAX_SIZE] = {0, };

/* initialize variables */
int N, M, r, c, d = 0; /* (N, M) : size of arr , (r, c): position of cleaner, d : direction */


int main() {
    cin >> N >> M >> r >> c >> d;
}
